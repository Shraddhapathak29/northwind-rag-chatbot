# northwind-rag-chatbot
Dual-mode agentic RAG chatbot that answers questions over company docs (vector RAG with citations) and an orders table (text-to-SQL), auto-routing each question to the right source. FastAPI + SSE streaming, Next.js UI, pgvector, open-source LLM via Hugging Face.
