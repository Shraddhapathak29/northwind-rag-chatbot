# --- Web / API ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
sse-starlette==2.1.3
pydantic==2.10.4
pydantic-settings==2.7.1

# --- LLM (chat / tool calling / streaming via OpenAI-compatible endpoint) ---
openai==1.59.6
tenacity==9.0.0          # retry/backoff on transient API errors

# --- Embeddings (local, open-source; ONNX runtime, no API => no rate limits) ---
fastembed==0.6.1

# --- Postgres + pgvector ---
psycopg[binary]==3.2.3
psycopg-pool==3.2.4
pgvector==0.3.6

# --- Document ingestion ---
pypdf==5.1.0

# --- SQL safety ---
sqlparse==0.5.3

# --- Misc ---
python-dotenv==1.0.1
